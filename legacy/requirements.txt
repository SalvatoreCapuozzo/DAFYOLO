# Dependencies for the legacy DAFYOLO scripts at repo root: client.py /
# client_updated.py / client_v2.py, server.py / server_updated.py /
# server_v2.py, run_experiments.py, check_server_running.py,
# diagnose_connection.py. There was no requirements.txt for these before --
# they only had implicit imports.
#
# fedyolo_system/ has its own requirements.txt; install that separately if
# you're working on that variant instead.

torch>=2.1
torchvision>=0.16
ultralytics>=8.3
pyyaml>=6.0
paramiko>=3.0        # SSH/SFTP transport between client and server
python-dotenv>=1.0    # loads .env (SERVER_IP, USERNAME, PASSWORD)
